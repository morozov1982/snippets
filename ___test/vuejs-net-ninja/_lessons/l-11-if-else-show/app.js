new Vue({
    el: '#vue-app',
    data: {
        error: false,
        success: false
    },
    methods: { // Запускаются оба метода (функции)
        
    },
    computed: { // Запускается только та функция, которая необходима
        
    }
});